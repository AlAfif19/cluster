---
phase: 02-authentication-system
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified: [backend/app/api/v1/endpoints/auth.py, backend/app/core/deps.py, backend/app/api/v1/api.py]
autonomous: true
requirements: [AUTH-02, AUTH-05]

must_haves:
  truths:
    - "User can log in via POST /api/v1/auth/login with email and password"
    - "Login endpoint verifies credentials and returns JWT access token"
    - "Invalid credentials return 401 error with clear message"
    - "JWT tokens include email claim and expire after configured time"
    - "Protected routes require valid JWT token in Authorization header"
  artifacts:
    - path: "backend/app/api/v1/endpoints/auth.py"
      provides: "Login endpoint with JWT token generation"
      min_lines: 50
      exports: ["router", "login_for_access_token"]
      contains: "POST.*login", "create_access_token"
    - path: "backend/app/core/deps.py"
      provides: "Authentication dependency for protected routes"
      min_lines: 40
      exports: ["get_current_user", "get_current_active_user"]
      contains: "get_current_user", "get_db"
    - path: "backend/app/api/v1/api.py"
      provides: "API router with auth routes included"
      min_lines: 20
      contains: "include_router.*auth"
  key_links:
    - from: "backend/app/api/v1/endpoints/auth.py"
      to: "backend/app/models/user.py"
      via: "User model for credential verification"
      pattern: "from.*models.*user.*import.*User"
    - from: "backend/app/api/v1/endpoints/auth.py"
      to: "backend/app/core/security.py"
      via: "Password verification and JWT token creation"
      pattern: "from.*core.*security.*import.*verify_password, create_access_token"
    - from: "backend/app/core/deps.py"
      to: "backend/app/core/security.py"
      via: "JWT token verification"
      pattern: "from.*core.*security.*import.*verify_token"
    - from: "backend/app/api/v1/endpoints/auth.py"
      to: "backend/app/core/deps.py"
      via: "Database dependency injection"
      pattern: "Depends.*get_db"
---

<objective>
Create login API endpoint with JWT session management and token expiration

Purpose: Enable authenticated users to log in and receive JWT tokens for session management
Output: Working POST /api/v1/auth/login endpoint with JWT token generation and protected route dependencies
</objective>

<execution_context>
@C:/Users/Abdurr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Abdurr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@backend/app/models/user.py
@backend/app/schemas/user.py
@backend/app/database.py
@backend/app/core/security.py
@.env.example

# From Plan 02-01 (User Model & Password Hashing)

From backend/app/models/user.py:
- User model with id, email, hashed_password, full_name, is_active, created_at, updated_at
- Password functions: verify_password(), get_password_hash()

From backend/app/schemas/user.py:
- UserLogin schema with email, password
- User response schema
- Token schema with access_token, token_type
- TokenData schema for JWT payload

From backend/app/database.py:
- get_db() dependency for database session injection

From backend/app/core/security.py (from Plan 02-02):
- verify_password(), get_password_hash()
- create_access_token() for JWT generation
- SECRET_KEY, ALGORITHM, ACCESS_TOKEN_EXPIRE_MINUTES constants
- oauth2_scheme for OAuth2 password flow

From .env.example:
- SECRET_KEY for JWT signing
- ACCESS_TOKEN_EXPIRE_MINUTES=30 for token expiration
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create authentication dependencies for protected routes</name>
  <files>backend/app/core/deps.py</files>
  <action>
Create backend/app/core/deps.py (create core directory if needed) with authentication dependencies:

1. Import required modules:
   - FastAPI: Depends, HTTPException, status
   - SQLAlchemy: Session
   - From backend.app.database: get_db
   - From backend.app.models.user: User
   - From backend.app.core.security: verify_token, oauth2_scheme

2. Create credentials exception:
   - credentials_exception = HTTPException(
         status_code=status.HTTP_401_UNAUTHORIZED,
         detail="Could not validate credentials",
         headers={"WWW-Authenticate": "Bearer"},
     )

3. Create get_current_user dependency:
   @app.get("/users/me/", response_model=User)
   async def read_users_me(current_user: User = Depends(get_current_user)):
       return current_user

   Actually, the function is:

   def get_current_user(token: str = Depends(oauth2_scheme), db: Session = Depends(get_db)) -> User:
       """
       Verify JWT token and return current user.
       """
       # Verify token and extract email
       email = verify_token(token, credentials_exception)

       # Query user from database
       user = db.query(User).filter(User.email == email).first()
       if user is None:
           raise credentials_exception

       return user

4. Create get_current_active_user dependency:
   def get_current_active_user(current_user: User = Depends(get_current_user)) -> User:
       """
       Get current active user (user must be active).
       """
       if not current_user.is_active:
           raise HTTPException(status_code=400, detail="Inactive user")
       return current_user

5. Create optional get_current_user for routes where auth is optional:
   def get_current_user_optional(token: str = Depends(oauth2_scheme), db: Session = Depends(get_db)) -> Optional[User]:
       """
       Verify JWT token and return current user if token provided, None otherwise.
       Returns None instead of raising exception for optional auth.
       """
       try:
           email = verify_token(token, credentials_exception)
           user = db.query(User).filter(User.email == email).first()
           return user if user else None
       except HTTPException:
           return None

DO NOT: Allow inactive users through get_current_active_user
DO NOT: Return sensitive information (password, hashed_password) in user responses
</action>
  <verify>
    <automated>python -c "from backend.app.core.deps import get_current_user, get_current_active_user; print('Auth dependencies imported successfully')" 2>&1</automated>
  </verify>
  <done>Authentication dependencies created for protected and optional routes</done>
</task>

<task type="auto">
  <name>Task 2: Create login API endpoint with JWT token generation</name>
  <files>backend/app/api/v1/endpoints/auth.py</files>
  <action>
Create backend/app/api/v1/endpoints/auth.py with login endpoint:

1. Import required modules:
   - FastAPI: APIRouter, Depends, HTTPException, status
   - SQLAlchemy: Session
   - datetime: timedelta
   - From backend.app.database: get_db
   - From backend.app.models.user: User
   - From backend.app.schemas.user: Token, UserLogin
   - From backend.app.core.security: verify_password, create_access_token, ACCESS_TOKEN_EXPIRE_MINUTES
   - From backend.app.core.deps: get_current_user, get_current_active_user

2. Create router:
   - router = APIRouter()

3. Implement login endpoint:
   @router.post("/login", response_model=Token)
   def login_for_access_token(form_data: OAuth2PasswordRequestForm = Depends(), db: Session = Depends(get_db)):
       """
       Login with email and password to get JWT access token.
       """
       # Find user by email
       user = db.query(User).filter(User.email == form_data.username).first()
       if not user:
           raise HTTPException(
               status_code=status.HTTP_401_UNAUTHORIZED,
               detail="Incorrect email or password",
               headers={"WWW-Authenticate": "Bearer"},
           )

       # Verify password
       if not verify_password(form_data.password, user.hashed_password):
           raise HTTPException(
               status_code=status.HTTP_401_UNAUTHORIZED,
               detail="Incorrect email or password",
               headers={"WWW-Authenticate": "Bearer"},
           )

       # Create access token
       access_token_expires = timedelta(minutes=ACCESS_TOKEN_EXPIRE_MINUTES)
       access_token = create_access_token(
           data={"sub": user.email}, expires_delta=access_token_expires
       )

       return {"access_token": access_token, "token_type": "bearer"}

4. Add protected endpoint to get current user:
   @router.get("/me", response_model=User)
   def read_users_me(current_user: User = Depends(get_current_active_user)):
       """
       Get current authenticated user information.
       """
       return current_user

5. Add endpoint to test if user is authenticated:
   @router.get("/verify")
   def verify_token_status(current_user: User = Depends(get_current_active_user)):
       """
       Verify current token and return user status.
       """
       return {
           "authenticated": True,
           "email": current_user.email,
           "is_active": current_user.is_active
       }

Note: OAuth2PasswordRequestForm is from fastapi.security import OAuth2PasswordRequestForm
It expects username field (we'll use email) and password field

DO NOT: Return user password or hashed_password in any response
DO NOT: Allow login for inactive users
DO: Use 401 status code for authentication failures
</action>
  <verify>
    <automated>MISSING — Wave 0 must create backend/tests/test_auth.py first</automated>
  </verify>
  <done>Login endpoint created with JWT token generation and protected user endpoints</done>
</task>

<task type="auto">
  <name>Task 3: Integrate auth router into main API</name>
  <files>backend/app/api/v1/api.py</files>
  <action>
Update backend/app/api/v1/api.py to include auth router:

1. Import auth router:
   - From backend.app.api.v1.endpoints.auth import router as auth_router

2. Include auth router in API:
   - api_router.include_router(auth_router, prefix="/auth", tags=["authentication"])

The updated api.py should include:
- Existing users router: api_router.include_router(users_router, prefix="/users", tags=["users"])
- New auth router: api_router.include_router(auth_router, prefix="/auth", tags=["authentication"])

Verify api.py structure:
```python
from fastapi import APIRouter

from backend.app.api.v1.endpoints.users import router as users_router
from backend.app.api.v1.endpoints.auth import router as auth_router

api_router = APIRouter()

api_router.include_router(users_router, prefix="/users", tags=["users"])
api_router.include_router(auth_router, prefix="/auth", tags=["authentication"])
```

DO NOT: Remove or modify existing users router integration
DO: Keep routers properly organized with prefixes and tags
</action>
  <verify>
    <automated>python -c "from backend.app.api.v1.api import api_router; print(f'Routes: {[r.path for r in api_router.routes]}')" 2>&1</automated>
  </verify>
  <done>Auth router integrated into main API at /api/v1/auth prefix</done>
</task>

</tasks>

<verification>
1. Auth dependencies test: Verify get_current_user and get_current_active_user work
2. Login endpoint test: POST valid credentials and verify 200 with JWT token
3. Invalid credentials test: POST wrong email/password and verify 401 error
4. Token verification test: Use JWT token to access /me endpoint
5. Expired token test: Use expired token and verify 401 error
6. Protected route test: Access /me without token and verify 401 error
7. Active user test: Verify inactive users cannot login
</verification>

<success_criteria>
- POST /api/v1/auth/login returns JWT access token for valid credentials
- Invalid credentials return 401 with "Incorrect email or password"
- GET /api/v1/auth/me returns current user with valid JWT token
- GET /api/v1/auth/me returns 401 without JWT token
- JWT tokens include email claim and expire after ACCESS_TOKEN_EXPIRE_MINUTES
- Inactive users cannot login or access protected routes
- API accessible at http://localhost:8000/api/v1/auth
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication-system/02-03-SUMMARY.md`
</output>
