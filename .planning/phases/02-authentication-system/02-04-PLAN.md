---
phase: 02-authentication-system
plan: 04
type: execute
wave: 3
depends_on: [02-03]
files_modified: [backend/app/api/v1/endpoints/auth.py]
autonomous: true
requirements: [AUTH-03]

must_haves:
  truths:
    - "User can log out via POST /api/v1/auth/logout endpoint"
    - "Logout endpoint accepts JWT token and invalidates it"
    - "Subsequent requests with logged-out token return 401 error"
    - "Logout is idempotent (calling multiple times returns success)"
  artifacts:
    - path: "backend/app/api/v1/endpoints/auth.py"
      provides: "Logout endpoint with token invalidation"
      min_lines: 20
      exports: ["router", "logout"]
      contains: "POST.*logout", "logout"
    - path: "backend/app/core/security.py"
      provides: "Token blacklist/invalidation mechanism"
      min_lines: 30
      exports: ["invalidate_token", "is_token_valid"]
      contains: "invalidate_token", "is_token_valid"
  key_links:
    - from: "backend/app/api/v1/endpoints/auth.py"
      to: "backend/app/core/security.py"
      via: "Token invalidation utilities"
      pattern: "from.*core.*security.*import.*invalidate_token"
    - from: "backend/app/core/deps.py"
      to: "backend/app/core/security.py"
      via: "Token validity check in get_current_user"
      pattern: "is_token_valid"
    - from: "backend/app/api/v1/endpoints/auth.py"
      to: "backend/app/core/deps.py"
      via: "get_current_user dependency"
      pattern: "Depends.*get_current_user"
---

<objective>
Create logout API endpoint with JWT token invalidation

Purpose: Enable authenticated users to log out and invalidate their JWT tokens
Output: Working POST /api/v1/auth/logout endpoint with token blacklist/invalidation
</objective>

<execution_context>
@C:/Users/Abdurr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Abdurr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@backend/app/api/v1/endpoints/auth.py
@backend/app/core/security.py
@backend/app/core/deps.py
@backend/app/schemas/user.py

# From Plan 02-03 (Login API & JWT Sessions)

From backend/app/api/v1/endpoints/auth.py:
- POST /api/v1/auth/login endpoint with JWT token generation
- GET /api/v1/auth/me endpoint for current user
- GET /api/v1/auth/verify endpoint for token verification
- Token schema with access_token and token_type

From backend/app/core/security.py:
- create_access_token(data, expires_delta): Creates JWT token
- verify_token(token, credentials_exception): Decodes and validates JWT
- SECRET_KEY, ALGORITHM, ACCESS_TOKEN_EXPIRE_MINUTES constants

From backend/app/core/deps.py:
- get_current_user(token, db): Verifies token and returns user
- get_current_active_user(current_user): Returns user only if active

From backend/app/schemas/user.py:
- User response schema
- Token schema

# Logout Strategy
Since JWTs are stateless, we need a token blacklist mechanism for logout:
1. Simple approach (v1): In-memory set of blacklisted token IDs
2. Production approach (v2+): Redis-based token blacklist
3. Alternative: Short token expiration (15-30 minutes) + client-side token deletion

For v1, we'll implement:
- Simple in-memory token blacklist (set of token signatures)
- Modified get_current_user to check blacklist before returning user
- Logout endpoint adds token to blacklist
- Token signature: hash of the raw JWT string (first 64 chars)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add token blacklist mechanism to security module</name>
  <files>backend/app/core/security.py</files>
  <action>
Update backend/app/core/security.py with token blacklist functionality:

1. Add token blacklist storage:
   - Create module-level set: token_blacklist = set()

2. Add utility functions for blacklist management:

   def get_token_signature(token: str) -> str:
       """
       Extract token signature for blacklist (first 64 chars of token).
       Using token prefix as signature is sufficient for v1.
       """
       import hashlib
       return hashlib.sha256(token.encode()).hexdigest()[:64]

   def invalidate_token(token: str) -> bool:
       """
       Add token to blacklist.
       Returns True if token was successfully invalidated.
       """
       try:
           signature = get_token_signature(token)
           token_blacklist.add(signature)
           return True
       except Exception as e:
           return False

   def is_token_blacklisted(token: str) -> bool:
       """
       Check if token is in blacklist.
       Returns True if token is blacklisted (invalid).
       """
       try:
           signature = get_token_signature(token)
           return signature in token_blacklist
       except Exception as e:
           return True  # Treat errors as blacklisted for security

   def clear_blacklisted_token(token: str) -> bool:
       """
       Remove token from blacklist (for testing or manual cleanup).
       Returns True if token was removed.
       """
       try:
           signature = get_token_signature(token)
           if signature in token_blacklist:
               token_blacklist.remove(signature)
               return True
           return False
       except Exception as e:
           return False

3. Update verify_token to check blacklist (optional enhancement):
   Modify existing verify_token function to also check blacklist after decoding:
   - After successful JWT decode, call is_token_blacklisted(token)
   - If blacklisted, raise credentials_exception

   OR: Keep blacklist check in get_current_user dependency (better separation)

DO NOT: Store full tokens in blacklist (use signatures only for memory efficiency)
DO NOT: Allow blacklisted tokens to pass verification
NOTE: In-memory blacklist is lost on server restart (acceptable for v1)
</action>
  <verify>
    <automated>python -c "from backend.app.core.security import invalidate_token, is_token_blacklisted, get_token_signature; sig = get_token_signature('test-token'); invalidate_token('test-token'); print(f'Blacklisted: {is_token_blacklisted(\"test-token\")}')" 2>&1</automated>
  </verify>
  <done>Token blacklist mechanism added with invalidate and check functions</done>
</task>

<task type="auto">
  <name>Task 2: Update auth dependencies to check token blacklist</name>
  <files>backend/app/core/deps.py</files>
  <action>
Update backend/app/core/deps.py to check token blacklist:

1. Import blacklist check function:
   - From backend.app.core.security import is_token_blacklisted

2. Update get_current_user function:
   Modify the existing get_current_user function to check blacklist before returning user:

   def get_current_user(token: str = Depends(oauth2_scheme), db: Session = Depends(get_db)) -> User:
       """
       Verify JWT token, check blacklist, and return current user.
       """
       # Verify token and extract email
       email = verify_token(token, credentials_exception)

       # Check if token is blacklisted (logged out)
       if is_token_blacklisted(token):
           raise HTTPException(
               status_code=status.HTTP_401_UNAUTHORIZED,
               detail="Token has been invalidated (user logged out)",
               headers={"WWW-Authenticate": "Bearer"},
           )

       # Query user from database
       user = db.query(User).filter(User.email == email).first()
       if user is None:
           raise credentials_exception

       return user

   The blacklist check happens AFTER JWT verification but BEFORE user lookup.

3. Update get_current_user_optional (if exists):
   Add similar blacklist check to the optional auth function:
   - Call is_token_blacklisted(token) before attempting user lookup
   - Return None if token is blacklisted

DO NOT: Skip blacklist check for any protected route
DO: Return specific error message for blacklisted tokens ("Token has been invalidated")
</action>
  <verify>
    <automated>python -c "from backend.app.core.deps import get_current_user; print('Auth dependency with blacklist check imported successfully')" 2>&1</automated>
  </verify>
  <done>Auth dependencies updated to check token blacklist before returning user</done>
</task>

<task type="auto">
  <name>Task 3: Create logout API endpoint</name>
  <files>backend/app/api/v1/endpoints/auth.py</files>
  <action>
Update backend/app/api/v1/endpoints/auth.py with logout endpoint:

1. Import required modules:
   - From backend.app.core.security import invalidate_token

2. Implement logout endpoint:
   @router.post("/logout")
   def logout(token: str = Depends(oauth2_scheme)):
       """
       Logout current user by invalidating JWT token.
       """
       success = invalidate_token(token)
       if success:
           return {"message": "Successfully logged out"}
       else:
           raise HTTPException(
               status_code=status.HTTP_500_INTERNAL_SERVER_ERROR,
               detail="Failed to logout"
           )

3. Add endpoint to logout all sessions (optional enhancement):
   @router.post("/logout-all")
   def logout_all(current_user: User = Depends(get_current_active_user)):
       """
       Logout all sessions for current user by blacklisting all tokens.
       Note: In v1 with simple blacklist, this clears the entire blacklist.
       For production, implement user-specific token tracking.
       """
       # For v1: Clear entire blacklist (all tokens)
       from backend.app.core.security import token_blacklist
       token_blacklist.clear()
       return {"message": "All sessions logged out"}

4. Add endpoint to check logout status:
   @router.get("/logout-status")
   def logout_status(token: str = Depends(oauth2_scheme)):
       """
       Check if current token has been logged out.
       """
       from backend.app.core.security import is_token_blacklisted
       is_blacklisted = is_token_blacklisted(token)
       return {
           "logged_out": is_blacklisted,
           "message": "Token is valid" if not is_blacklisted else "Token has been invalidated"
       }

5. Update existing /verify endpoint to check blacklist:
   @router.get("/verify")
   def verify_token_status(token: str = Depends(oauth2_scheme), db: Session = Depends(get_db)):
       """
       Verify current token and return user status.
       """
       from backend.app.core.security import is_token_blacklisted

       if is_token_blacklisted(token):
           return {
               "authenticated": False,
               "logged_out": True,
               "message": "Token has been invalidated"
           }

       user = get_current_user(token, db)
       return {
           "authenticated": True,
           "logged_out": False,
           "email": user.email,
           "is_active": user.is_active
       }

DO NOT: Require request body for logout (use Authorization header only)
DO: Make logout idempotent (calling multiple times returns success)
</action>
  <verify>
    <automated>MISSING — Wave 0 must create backend/tests/test_auth.py first</automated>
  </verify>
  <done>Logout endpoint created with token blacklist integration</done>
</task>

</tasks>

<verification>
1. Token blacklist test: Invalidate token and verify it's blacklisted
2. Logout endpoint test: POST /logout with valid token and verify success
3. Blacklisted token test: Try to access /me with blacklisted token and verify 401
4. Idempotent logout test: Call /logout multiple times and verify all succeed
5. Logout status test: Check /logout-status before and after logout
6. Verify endpoint test: Call /verify after logout and verify logged_out status
7. Protected route test: Access /me after logout and verify 401 error
</verification>

<success_criteria>
- POST /api/v1/auth/logout invalidates JWT token and returns success message
- Blacklisted tokens return 401 "Token has been invalidated (user logged out)"
- GET /api/v1/auth/me fails after logout with blacklisted token
- GET /api/v1/auth/logout-status shows logged_out: True after logout
- Logout is idempotent (multiple calls succeed)
- GET /api/v1/auth/verify shows logged_out: True after logout
- All protected routes reject blacklisted tokens with specific error message
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication-system/02-04-SUMMARY.md`
</output>
